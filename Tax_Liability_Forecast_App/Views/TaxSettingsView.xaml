<UserControl x:Class="Tax_Liability_Forecast_App.Views.TaxSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tax_Liability_Forecast_App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Tax Brackets" Foreground="White" Grid.Row="0" Margin="5">
            <StackPanel>
                <DataGrid ItemsSource="{Binding TaxBrackets}" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="From" Binding="{Binding From}" Width="*"/>
                        <DataGridTextColumn Header="To" Binding="{Binding To}" Width="*"/>
                        <DataGridTextColumn Header="Rate %" Binding="{Binding Rate}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" Command="{Binding DataContext.EditTaxBracketCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                        <Button Content="Delete" Command="{Binding DataContext.DeleteTaxBracketCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Content="+ Add Bracket" Command="{Binding AddTaxBracketCommand}" HorizontalAlignment="Left" Margin="5"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Deduction Types" Foreground="White" Grid.Row="1" Margin="5">
            <StackPanel>
                <DataGrid ItemsSource="{Binding DeductionTypes}" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Is Deductible" Binding="{Binding IsDeductible}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" Command="{Binding DataContext.EditDeductionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                        <Button Content="Delete" Command="{Binding DataContext.DeleteDeductionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Content="+ Add Deduction" Command="{Binding AddDeductionCommand}" HorizontalAlignment="Left" Margin="5"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Tax Deadlines" Foreground="White" Grid.Row="2" Margin="5">
            <StackPanel>
                <DataGrid ItemsSource="{Binding TaxDeadlines}" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Period" Binding="{Binding Period}" Width="*"/>
                        <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Edit" Command="{Binding DataContext.EditDeadlineCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                        <Button Content="Delete" Command="{Binding DataContext.DeleteDeadlineCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Content="+ Add Deadline" Command="{Binding AddDeadlineCommand}" HorizontalAlignment="Left" Margin="5"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
