<UserControl x:Class="Tax_Liability_Forecast_App.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tax_Liability_Forecast_App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="Root">
        <StackPanel Orientation="Horizontal" Width="700px" Height="20px" HorizontalAlignment="Center" Margin="0,0,0,425" Background="White">
            <TextBox Width="680" Text="Search"></TextBox>
            <TextBlock Width="20px" Text="🔍"></TextBlock>
        </StackPanel>
        <DataGrid ItemsSource="{Binding Clients1}" Width="700" Height="300" HorizontalAlignment="Center" Margin="0,0,0,100" 
                  AutoGenerateColumns="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Phone Number" Binding="{Binding PhoneNum}" Width="*"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Add" Width="40px" HorizontalAlignment="Left" Margin="0,0,0,0" Command="{Binding DataContext.AddBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                                <Button Content="Edit" Width="40px" HorizontalAlignment="Left" Margin="2,0,0,0" Command="{Binding DataContext.EditBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                                <Button Content="Delete" Width="40px" HorizontalAlignment="Left" Margin="2,0,0,0" Command="{Binding DataContext.RemoveBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Width="170px" Height="30px" Orientation="Horizontal" Margin="530,240,0,0" HorizontalAlignment="Center">
            <Button Content="Add" Width="50" Height="30" HorizontalAlignment="Left" Margin="0,0,0,0" Command="{Binding AddBtnClick}"/>
            <Button Content="Edit" Width="50" Height="30" HorizontalAlignment="Left" Margin="10,0,0,0" Command="{Binding EditBtnClick}"/>
            <Button Content="Delete" Width="50" Height="30" HorizontalAlignment="Left" Margin="10,0,0,0" Command="{Binding RemoveBtnClick}"/>
        </StackPanel>
    </Grid>
</UserControl>
