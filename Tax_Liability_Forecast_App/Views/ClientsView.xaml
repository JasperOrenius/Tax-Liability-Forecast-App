<UserControl x:Class="Tax_Liability_Forecast_App.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tax_Liability_Forecast_App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" Margin="10,10,10,0" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20px"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="Search"></TextBox>
            <TextBlock Grid.Column="1" Text="🔍"></TextBlock>
        </Grid>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Clients1}" HorizontalAlignment="Center" Margin="10"
                  AutoGenerateColumns="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client Name" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Phone Number" Binding="{Binding PhoneNum, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Add" Width="40px" HorizontalAlignment="Left" Margin="0,0,0,0" Command="{Binding DataContext.AddBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                                <Button Content="Edit" Width="40px" HorizontalAlignment="Left" Margin="2,0,0,0" Command="{Binding DataContext.EditBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                                <Button Content="Delete" Width="40px" HorizontalAlignment="Left" Margin="2,0,0,0" Command="{Binding DataContext.RemoveBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
