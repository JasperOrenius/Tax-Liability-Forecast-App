<UserControl x:Class="Tax_Liability_Forecast_App.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tax_Liability_Forecast_App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" Margin="10,10,10,0" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20px"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="Search"></TextBox>
            <TextBlock Grid.Column="1" Text="🔍"></TextBlock>
        </Grid>
        <GroupBox Grid.Row="1" Margin="10" Header="Add client" Foreground="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center">Name</TextBlock>
                <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center">Email</TextBlock>
                <TextBlock Grid.Row="0" Grid.Column="2" HorizontalAlignment="Center">Phone Number</TextBlock>

                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding ClientNameInput, Mode=TwoWay}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="10,0,0,0" Text="{Binding ClientEmailInput, Mode=TwoWay}"/>
                <TextBox Grid.Row="1" Grid.Column="2" Margin="10,0,0,0" Text="{Binding ClientPhoneNumInput, Mode=TwoWay}"/>

                <Button Grid.Row="2" Grid.ColumnSpan="3" Margin="0,10,0,10" Content="Add client" Command="{Binding AddBtnClick}"></Button>
            </Grid>
            
            <!--<StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="80,0,0,0">Name</TextBlock>
                    <TextBlock Margin="180,0,0,0">Email</TextBlock>
                    <TextBlock Margin="160,0,0,0">Phone Number</TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="200px" Text="{Binding ClientNameInput, Mode=TwoWay}"/>
                    <TextBox Width="200px" Margin="10,0,0,0" Text="{Binding ClientEmailInput, Mode=TwoWay}"/>
                    <TextBox Width="200px" Margin="10,0,0,0" Text="{Binding ClientPhoneNumInput, Mode=TwoWay}"/>
                </StackPanel>
                <Button Content="Add client" Command="{Binding AddBtnClick}"></Button>
            </StackPanel>-->
        </GroupBox>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Clients}" HorizontalAlignment="Center" Margin="10"
                  AutoGenerateColumns="False" SelectionMode="Single" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client Name" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Phone Number" Binding="{Binding PhoneNum, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="{Binding DataContext.EditBtnText, ElementName=Root}" Width="40px" HorizontalAlignment="Left" Margin="2,0,0,0" Command="{Binding DataContext.EditBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                                <Button Content="Delete" Width="40px" HorizontalAlignment="Left" Margin="2,0,0,0" Command="{Binding DataContext.RemoveBtnClick, ElementName=Root}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
